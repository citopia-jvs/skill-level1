export interface RawUserInfo {
  data: {
    first_name: string;
    last_name: string;
    avatar: string;
  };
}
export interface UserInfo {
  firstName: string;
  lastName: string;
  avatar: string;
}
